import { intEqual } from '../../../helpers/int-equal';
import { module, test } from 'qunit';

module('Unit | Helper | int equal');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = intEqual(42);
  assert.ok(result);
});
